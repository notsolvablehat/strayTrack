specVersion: "v1beta"
serviceId: "internship"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "internship-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
